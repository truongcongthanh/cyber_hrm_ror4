<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Cyber Hrm Rorv4" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Cyber Hrm Rorv4" %>">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  <%= csrf_meta_tags %>
</head>
<body>
<header>
  <%= render 'layouts/navigation' %>
</header>
<div class="container">
  <div class="row">
    <div class="col-lg-3">
      <aside>
        <%= render 'sidebar' %>
      </aside>
    </div>
    <div class="col-lg-9">
      <main role="main">
        <div id="message">
          <%= render 'layouts/messages' %>
        </div>
        <div id="body-content">
          <%= yield %>
        </div>
      </main>
    </div>
  </div>
</div>
</body>
</html>
